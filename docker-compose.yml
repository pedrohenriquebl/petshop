services:
  postgres:
    image: postgres:17
    container_name: petshop-db
    restart: always
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: petshop
